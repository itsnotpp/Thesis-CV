{
  "name": "eenp-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "postinstall": "cd apps/web && prisma generate"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
